import React, { Component } from 'react';import {View, Text, Image, TouchableOpacity, FlatList } from 'react-native';import { Icon, Body, Card, CardItem } from 'native-base';import colors from "../consts/colors";class CartProduct extends Component{	constructor(props){		super(props);		this.state = {			quantity: this.props.product.quantity,		}	}	setQuantity(price){		this.props.calcTotal(price);		if (price > 0)			this.state.quantity == this.props.product.stock ? this.state.quantity : this.setState({ quantity: this.state.quantity + 1 })		else			this.state.quantity == 1 ? this.state.quantity : this.setState({ quantity: this.state.quantity - 1 })	}	render() {		return (			<Card>				<CardItem>					<Body onPress={() => this.props.navigation.navigate('product')}>						<View>							<Image source={{uri: this.props.product.logo}} resizeMode="center" style={{width: 100, height: 100}}/>						</View>					</Body>					<Body style={{flex: 2, marginLeft: 20}}>						<TouchableOpacity onPress={() => this.props.deleteProduct()} style={{position: 'absolute', right: -15}}>							<Icon type="Entypo" name="cross" style={{color: colors.gray}}/>						</TouchableOpacity>						<View>							<Text onPress={() => this.props.navigation.navigate('product')} style={{ fontSize: 16, fontWeight: '400', color: colors.gray }}>{ this.props.product.name.substr(0, 25) + '...'}</Text>							<Text style={{color: colors.gray}}>{ this.props.product.price } ريال سعودي</Text>							<Text style={{color: '#545454', fontSize: 16, fontWeight: '400'}}>الكمية</Text>							<View style={{flexDirection: 'row', justifyContent: 'space-between', width: 100, marginTop: 10}}>								<TouchableOpacity onPress={() => this.setQuantity(this.props.product.price)} style={{borderRadius: 5, backgroundColor: '#ededed', padding: 2}}>									<Icon name="plus" type="Feather" style={{color: colors.gray, fontSize: 20}}/>								</TouchableOpacity>								<Text style={{paddingHorizontal: 5, fontSize: 15}}>{ this.state.quantity }</Text>								<TouchableOpacity onPress={() => this.setQuantity(-(this.props.product.price))} style={{borderRadius: 5, backgroundColor: '#ededed', padding: 2}}>									<Icon name="minus" type="Feather" style={{color: colors.gray, fontSize: 20}}/>								</TouchableOpacity>							</View>						</View>					</Body>				</CardItem>			</Card>		)	}}export default CartProduct;