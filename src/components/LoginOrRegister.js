import React, { Component } from 'react';import {	View,	Text,	KeyboardAvoidingView,	Image,	TouchableOpacity,	ImageBackground,	BackHandler,	Dimensions} from 'react-native';import { Container, Button, Content, Form, Item, Input, Icon, Toast } from 'native-base';import { Spinner } from '../common'import { connect } from 'react-redux';import { userLogin, profile } from '../actions'import { Permissions, Notifications } from 'expo';const width = Dimensions.get('window').width;class LoginOrRegister extends Component{	constructor(props){		super(props);	}	componentDidMount() {		BackHandler.addEventListener('hardwareBackPress', this.handleBackPress);	}	handleBackPress = () => {		if (this.state.routeName === 'home'){			BackHandler.exitApp();			return true		}else			this.goBack();	};	goBack(){		this.props.navigation.goBack();	}	renderLoading(){		if (this.state.loader){			return(<Spinner />);		}		return (			<Button block style={{ position: 'absolute', backgroundColor: '#eebc47', width: '100%', height: 40 ,alignSelf: 'center', borderRadius: 0, justifyContent: 'center'}} onPress={() => { this.onLoginPressed()  }} primary>				<Text style={{color: '#fff', fontSize: 17, textAlign: 'center'}}>دخول</Text>			</Button>		);	}	render(){		return(			<Container style={{ backgroundColor: '#2d4058' }}>				<Content contentContainerStyle={{flexGrow: 1}}>					<View style={{justifyContent: 'center', alignItems: 'center', paddingTop: 70, padding: 20}}>						<Image resizeMode={'center'} style={{width: (70/100)*width , height: (70/100)*width, marginBottom: 50}}							source={require('../../assets/images/logo.png')}/>						<Button style={{ backgroundColor: '#ea5454', marginBottom: 20 }} onPress={() => this.props.navigation.navigate('login')} block>							<Text style={{ color: '#fff' }}>تسجيل دخول</Text>						</Button>						<Button style={{ backgroundColor: '#f0ba43' }} block onPress={() => this.props.navigation.navigate('signUp')}>							<Text style={{ color: '#fff' }}>انشاء حساب جديد</Text>						</Button>					</View>				</Content>			</Container>		)	}}export default LoginOrRegister;